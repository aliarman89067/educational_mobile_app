import React from "react";
import { StyleSheet, Text, View } from "react-native";

import {
  atom,
  bag,
  book,
  calculator,
  clock,
  football,
  hat,
  paint,
  paperRoll,
  pencil,
  scale,
  scaller,
  scissor,
  worldBall,
} from "@/constants/assetsImages";

const ScreenAssetsAnimation = () => {
  return <></>;
};

export default ScreenAssetsAnimation;

const styles = StyleSheet.create({});
